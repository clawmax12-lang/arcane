# Trade — environment variable template (SAFE to commit; no real values).
# Copy to .env and fill in during CLAUDE.md §0.5 onboarding. Never put real keys here.

# ── Tier 1 data-stack ──
FIRECRAWL_API_KEY=
TAVILY_API_KEY=
APIFY_TOKEN=
POLYGON_API_KEY=

# ── Broker (Alpaca paper) — executor/scheduler via alpaca-py SDK ──
APCA_API_KEY_ID=
APCA_API_SECRET_KEY=
APCA_API_BASE_URL=https://paper-api.alpaca.markets

# ── LLM (slow-loop agentic calls from Python) ──
ANTHROPIC_API_KEY=
# Inc-8.5 model selection (optional; blank => defaults). The two-way console CONVERSATION runs on
# Sonnet (claude-sonnet-4-6); flip to claude-opus-4-8 for the deepest answers, or to
# claude-haiku-4-5-20251001 to cut cost. The cheap structured agents default to Haiku.
CONSOLE_MODEL_ID=
AGENT_MODEL_ID=

# ── Macro / research ──
FRED_API_KEY=
GITHUB_TOKEN=

# ── Alerts ──
DISCORD_WEBHOOK_URL=
# Telegram (the §5 Murphy pager + §1.1 daily-report channel; disaster-recovery transport).
# Bot @Traderexperimentbot. CHAT_ID auto-resolves via getUpdates if blank (message the bot once).
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=
